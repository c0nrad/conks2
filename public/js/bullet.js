// Generated by CoffeeScript 1.7.1
(function() {
  var Bullet;

  Bullet = (function() {
    function Bullet(X, Y, direction, scene, color) {
      this.X = X;
      this.Y = Y;
      this.direction = direction;
      this.scene = scene;
      this.color = color;
      this.geometry = new THREE.CubeGeometry(1, 1, 1);
      this.material = new THREE.MeshBasicMaterial({
        color: this.color
      });
      this.cube = new THREE.Mesh(this.geometry, this.material);
      this.cube.position.z = 5;
      this.cube.position.x = this.X;
      this.cube.position.y = this.Y;
      this.alive = true;
      this.scene.add(this.cube);
    }

    Bullet.prototype.remove = function() {
      return this.scene.remove(this.cube);
    };

    Bullet.prototype.move = function() {
      this.cube.position.x -= 5 * Math.cos(this.direction + Math.PI / 2);
      return this.cube.position.y -= 5 * Math.sin(this.direction + Math.PI / 2);
    };

    Bullet.prototype.boundsCheck = function() {
      var radi;
      radi = window.MAP_LENGTH / 2;
      if (this.cube.position.x > radi || this.cube.position.x < -radi) {
        return false;
      }
      if (this.cube.position.y > radi || this.cube.position.y < -radi) {
        return false;
      }
      return true;
    };

    return Bullet;

  })();

  window.Bullet = Bullet;

}).call(this);

// Generated by CoffeeScript 1.7.1
(function() {
  var Monster;

  Monster = (function() {
    function Monster(X, Y, scene) {
      this.scene = scene;
      console.log("MONSTA!!!", X, Y);
      this.geometry = new THREE.CubeGeometry(10, 10, 10);
      this.material = new THREE.MeshBasicMaterial({
        color: 0xFFFF00
      });
      this.cube = new THREE.Mesh(this.geometry, this.material);
      this.cube.position.z = 30;
      this.cube.position.x = X;
      this.cube.position.y = Y;
      this.counter = 0;
      this.alive = true;
      this.scene.add(this.cube);
      this.bullets = [];
    }

    Monster.prototype.remove = function() {
      return this.scene.remove(this.cube);
    };

    Monster.prototype.update = function(pX, pY) {
      return this.move(pX, pY);
    };

    Monster.prototype.move = function(pX, pY) {
      var dX, dY, normalize;
      if (this.cube.position.z > 5) {
        this.cube.position.z -= 5;
        return;
      }
      dX = pX - this.cube.position.x;
      dY = pY - this.cube.position.y;
      normalize = Math.sqrt(dX * dX + dY * dY);
      this.cube.position.x += dX / normalize * 3;
      return this.cube.position.y += dY / normalize * 3;
    };

    return Monster;

  })();

  window.Monster = Monster;

}).call(this);
